# 어학연수 다기능 편의 앱 — 기획 문서

> 이 문서는 개발 과정 전반의 기준 문서입니다. 새로운 대화 세션에서도 이 문서를 먼저 읽으면 지금까지의 결정 사항을 파악할 수 있도록 유지합니다. 결정이 바뀌면 이 문서도 함께 갱신합니다.

## 1. 프로젝트 개요

- **목적**: 해외 어학연수 중 사용할 개인용 다기능 편의 앱
- **사용자**: 개발자 본인 1인 사용 (다중 사용자/로그인 시스템 없음)
- **최종 결과물**: `.apk` 파일 (Android)
- **개발 방식**: 사용자와 Claude가 끝까지 함께 개발. Android Studio는 필요할 때만 사용하고, 기본적으로 Claude Code(CLI/Gradle)로 코드 작성·빌드 진행
- **원본 요구사항 메모**: `요구사항.txt` (초기 아이디어 스케치, 이 문서가 이를 구체화한 버전)

## 2. 기술 스택

| 항목 | 결정 |
|---|---|
| 언어 | Kotlin (네이티브 Android) |
| UI 프레임워크 | Jetpack Compose |
| 로컬 데이터 저장 | Room (SQLite 기반), 기능별 테이블을 하나의 DB에 구성 |
| 백엔드/서버 | 없음. 로그인·회원 시스템 없음 |
| 외부 연동 | Phase 2부터 REST API / 웹 스크래핑으로 선택적 연동 (온라인일 때만 동작, 오프라인 시 마지막 캐시 값 표시) |
| minSdkVersion | 추천 26 (Android 8.0) — 기기 커버리지와 최신 API 편의성의 균형. 추후 변경 가능 |
| 앱 이름 | **하민이 꽃신 신겨주기** |
| 패키지 컨셉명 | Travel Utility App (개념적 프로젝트명) |
| 실제 Android 패키지 ID | 미정 — Android는 `com.example.app` 형식(소문자/점 표기, 공백·대문자 불가)만 허용하므로, 프로젝트 생성 시 위 컨셉명을 바탕으로 확정 필요 (예: `com.hahamin.travelutility`) |

### 디자인/와이어프레임 워크플로우
- 사용자가 화면 와이어프레임을 그려서 전달할 수 있음
- **PNG, JPG, PDF** 등 범용 포맷으로 내보내기(export) 권장 — Claude가 직접 읽고 Compose 코드로 반영 가능
- 특정 노트 앱의 자체 포맷(예: `.pen` 등 proprietary 포맷)은 Claude가 직접 파싱하지 못할 수 있으므로, 어떤 앱으로 작업할지 확인 후 내보내기 포맷을 맞출 것

## 3. 기능 로드맵

### Phase 1 — MVP (완전 오프라인, Room DB 로컬 저장, 서버/로그인 없음)

**구현 순서: 날짜 계산기 → 체크리스트 → 단어장 → 환율 계산기(해외 가계부) → 일정 관리(조건부)**

1. **날짜 계산기**
   - 시작일 · 종료일 선택 (예: 9/6 ~ 11/28)
   - 오늘 날짜 기준으로 다양한 방식 표시:
     - 남은 일수 (D-day)
     - 남은 주 수
     - 전체 기간 대비 진행률 (%)

2. **체크리스트**
   - 출국 전 준비물 / 도착 후 할 일 템플릿 제공
   - 커스텀 항목 추가·수정·체크

3. **단어장 / 플래시카드**
   - 개인 단어 등록 (단어, 뜻, 예문 등)
   - 복습 퀴즈 기능 (간단한 간격 반복 방식 고려) 
   - 예시 단어 세트를 저장할 예정 

4. **해외 가계부 (환율 계산기)**
   - 지출 기록 (금액, 카테고리, 날짜, 메모)
   - 단위 선택 기능 넣기 (페소 / 원) 선택시 환율 반영해서 내역 표시.
   - 환율 수동 입력 계산기 (원화 ↔ 현지 통화)

5. **일정 관리** *(조건부 — 체크리스트 구성 결과에 따라 구현하지 않을 수도 있음)*
   - 수업 시간표 (직접 입력 방식으로 시작, Phase 2에서 LMS 가져오기 추가)
   - 개인 캘린더
   - 로컬 알림 (수업/과제 알림)

> ~~긴급 정보(대사관/응급실 연락처)~~ — 논의 후 Phase 1에서 제외 결정

### Phase 2 — 온라인 연동 (온라인일 때만 최신화, 오프라인 시 마지막 캐시 데이터 표시)

1. **실시간 환율 API 연동** (예: exchangerate-api.com 등 무료 티어)
2. **날씨 정보 API 연동**
3. **시간표 자동 가져오기** — 지정된 LMS 사이트(`lms.jicportal.com`)에서 시간표 정보를 읽어와 앱의 "일정 관리"에 반영 — **구현 완료 (2026-09-14)**
   - `POST /s/portal/schedule_ajax.html` (payload: `req_type=get_schedule_list`, 그 외 파라미터 없음)이 세션 쿠키 인증 기반 JSON API로 "현재 활성 기간"의 시간표 전체를 반환함
   - 응답에 `ssc_room`(강의실), `ssc_course`(과목명), `te_name`(담당 교사), `stb_name`("HH:mm ~ HH:mm" 시간 문자열)이 포함됨 → `ScheduleEntry`에 `course`/`teacher` 필드 추가, 표시·수동편집 UI 모두 한 항목당 2줄(1줄: 유형/시간/강의실, 2줄: 과목/교사)로 확장
   - Group/1on1 구분은 명시적 필드가 없어 `ssc_sgp_id`(그룹) vs `ssc_st_id`(1on1) 값 유무로 유추함
   - 사이트가 Cloudflare 봇 차단 뒤에 있어(`cf_clearance` 쿠키 필요), 순수 백그라운드 HTTP 요청 대신 **WebView로 로그인시키고 그 안에서 JS로 직접 fetch 호출** → 받은 JSON을 JavascriptInterface로 Kotlin에 전달하는 방식으로 구현 (`ui/schedule/LmsImportDialog.kt`)
   - 자동 주기 동기화는 하지 않음 — 사용자가 "가져오기" 버튼을 누를 때만 WebView를 띄우고, 가져온 데이터는 기존 시간표를 **완전히 덮어씀**
   - 로그인 자격증명은 앱이 전혀 다루지 않음 (WebView 안에서 사용자가 직접 로그인), 별도 암호화 저장 불필요해짐
4. **출석률 가져오기** — 같은 사이트에서 출석률 정보를 읽어와 앱에 반영

> ~~번역 기능~~, ~~길찾기/지도~~ — 논의 후 제외 결정

**Phase 2 착수 전 확인 필요 사항 (TODO)** — 아래 모두 확인 완료, 시간표 가져오기 기능 구현됨
- [x] 시간표 페이지의 데이터 구조 파악 — HTML 스크래핑이 아니라 `schedule_ajax.html` JSON API로 확인됨 (위 3번 항목 참고)
- [x] `schedule_ajax.html` 요청의 Request Payload 확인 — `req_type=get_schedule_list` 단일 파라미터, 날짜/식별자 불필요 (세션이 곧 식별자)
- [x] 로그인 방식은 별도 분석 없이 WebView에 실제 로그인 페이지를 그대로 띄우는 방식으로 우회 — 앱이 로그인 폼 구조를 알 필요 자체가 없어짐
- 사이트가 HTTP(비암호화)인지 HTTPS인지 확인 — 실제 접속해보니 `https://lms.jicportal.com`으로 리다이렉트됨 (메모상 `http://`였으나 HTTPS 사용 확인됨, 우려 해소)
- Cloudflare 봇 차단이 걸려있어 순수 HTTP 클라이언트로는 차단될 수 있음 → WebView 내 JS 실행 방식으로 우회 (위 3번 항목 참고)

**⚠️ 보안 참고사항**
- LMS 로그인 자격증명은 **소스코드에 하드코딩하지 않음**
- 앱 내 설정 화면에서 사용자가 직접 입력 → `EncryptedSharedPreferences` 등으로 로컬 암호화 저장
- `요구사항.txt`에 평문으로 남아있는 로그인 정보는, 이후 이 프로젝트를 git으로 관리하게 될 경우 반드시 `.gitignore` 처리하거나 파일에서 제거할 것

### Phase 3 — 확장 (선택 사항, 우선순위 낮음)

- 홈 화면 위젯
- 다국어 UI 지원 — **구현 완료 (2026-09-15)**
  - 화면에 하드코딩되어 있던 한글 문자열을 전부 `values/strings.xml`(한국어, 기본값) / `values-en/strings.xml`(영어)로 리소스화
  - "N분 뒤 수업 시작" ↔ "Class starts in N minutes." 같은 어순이 다른 문장도 언어별 포맷 문자열(`%1$d` 위치만 다르게 지정)로 자연스럽게 해결됨 — 코드에서 언어를 분기 처리할 필요 없음
  - 언어 선택은 홈 화면 톱니바퀴(설정) 버튼 → 설정 화면에서 시스템 기본/한국어/English 중 선택 (`LanguagePreferences`에 저장, `MainActivity.attachBaseContext`에서 적용, 변경 시 액티비티 재생성으로 즉시 반영)
- 로컬 백업/복원 (파일 export/import)
- 다크모드

## 4. 결정된 사항 요약 (변경 이력)

- 기술 스택: Kotlin 네이티브 + Jetpack Compose + Room, 서버 없음 (확정)
- 초기 개발 범위: Phase 1(오프라인 MVP)부터 순차 개발 (확정)
- 커뮤니티/로그인 기능: 불필요, 완전 개인용 (확정)
- 날짜 계산기: Phase 1에 추가 (확정)
- 긴급 정보 기능: Phase 1에서 제외 (확정)
- 번역/길찾기: Phase 2에서 제외, 대신 LMS 시간표·출석률 가져오기로 대체 (확정)
- 앱 이름: "하민이 꽃신 신겨주기", 패키지 컨셉명: "Travel Utility App" (확정, 실제 패키지 ID는 프로젝트 생성 시 확정)
- Phase 1 구현 순서: **날짜 계산기 → 체크리스트 → 단어장 → 환율 계산기 → 일정 관리(조건부)** (확정)

## 5. 아직 정해지지 않은 것 (Open TODO)

- [ ] 실제 Android 패키지 ID 확정 (예: `com.hahamin.travelutility`)
- [ ] 와이어프레임 작성 도구 확정 — pen.dev 사용 예정이나 내보내기 포맷 미확인 (사용자가 `.pen` 파일 준비 후 다시 논의하기로 함)
- [ ] LMS 사이트(`lms.jicportal.com`) 구조 분석 — 시간표 API 구조는 확인 완료, 로그인 플로우와 요청 Payload는 미확인 (2절 3번 항목 참고)
- [ ] 정확한 minSdkVersion / 타겟 기기 확정

## 6. 진행 상태

- **현재 단계: 기획 완료, 구현 시작 전** (2026-09-13 기준)
- 다음 세션에서 시작할 작업: Android Studio 프로젝트 스캐폴딩(Kotlin + Compose) → **날짜 계산기** 기능부터 구현 (화면 → 로직 → 필요 시 Room 저장 순서)
